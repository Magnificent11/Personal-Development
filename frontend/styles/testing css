/* Greek-Inspired Color Palette */
:root {
    --aegean-deep: #0B5394;
    --aegean-blue: #1E88E5;
    --aegean-light: #64B5F6;
    --sky-blue: #90CAF9;
    --white-marble: #FAFAFA;
    --pure-white: #FFFFFF;
    --text-dark: #1A1A1A;
    --text-medium: #4A4A4A;
    --text-light: #757575;
    --error-red: #E53935;
    --success-green: #43A047;
    --glass-white: rgba(255, 255, 255, 0.25);
    --glass-border: rgba(255, 255, 255, 0.4);
}

* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
    background: linear-gradient(135deg, #E3F2FD 0%, #FAFAFA 50%, #BBDEFB 100%);
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    position: relative;
    overflow: hidden;
}

/* Subtle Background Pattern */
body::before {
    content: '';
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: 
        radial-gradient(circle at 20% 50%, rgba(30, 136, 229, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 80% 80%, rgba(100, 181, 246, 0.08) 0%, transparent 50%),
        radial-gradient(circle at 40% 20%, rgba(11, 83, 148, 0.05) 0%, transparent 50%);
    animation: float 20s ease-in-out infinite;
    pointer-events: none;
}

@keyframes float {
    0%, 100% { transform: translate(0, 0) rotate(0deg); }
    33% { transform: translate(30px, -30px) rotate(1deg); }
    66% { transform: translate(-20px, 20px) rotate(-1deg); }
}

.container {
    width: 100%;
    max-width: 450px;
    z-index: 1;
}

/* Glassmorphism Card */
.auth-card {
    background: var(--glass-white);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border-radius: 24px;
    border: 1px solid var(--glass-border);
    box-shadow: 
        0 8px 32px rgba(11, 83, 148, 0.1),
        0 2px 8px rgba(0, 0, 0, 0.05),
        inset 0 1px 0 rgba(255, 255, 255, 0.5);
    padding: 40px;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.auth-card:hover {
    transform: translateY(-2px);
    box-shadow: 
        0 12px 40px rgba(11, 83, 148, 0.15),
        0 4px 12px rgba(0, 0, 0, 0.08),
        inset 0 1px 0 rgba(255, 255, 255, 0.5);
}




/* Center subtitle */
.centered {
    text-align: center;
    margin-top: -5px;
    margin-bottom: 20px;
}

/* Forms */
.auth-form {
    display: none;
    animation: fadeIn 0.4s ease;
}

.auth-form.active {
    display: block;
}

@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateY(10px);
    }
    to {
        opacity: 1;
        transform: translateY(0);
    }
}

/* Input Groups */
.input-group {
    margin-bottom: 16px; /* FIXED SPACING */
}

.input-group label {
    display: block;
    font-size: 14px;
    font-weight: 500;
    color: var(--text-dark);
    margin-bottom: 8px;
}

.input-group input {
    width: 100%;
    padding: 14px 16px;
    border: 2px solid rgba(30, 136, 229, 0.2);
    border-radius: 10px;
    font-size: 15px;
    color: var(--text-dark);
    background: rgba(255, 255, 255, 0.7);
    transition: all 0.3s ease;
    outline: none;
}

/* Footer links inline */
.footer-links {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 10px;
    margin-top: 10px;
}

.footer-links a {
    font-size: 14px;
    font-weight: 500;
    color: var(--aegean-blue);
    text-decoration: none;
}

.footer-links a:hover {
    color: var(--aegean-deep);
    text-decoration: underline;
}

.separator {
    opacity: 0.5;
}
